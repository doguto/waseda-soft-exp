public class main {
    while(true)
    {
        switch () {
            case :
                service = new CreateRoomService();
                service.call();
                break;
            default:
                throw new AssertionError();
        }
    }
}
